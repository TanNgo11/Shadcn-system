import { Card } from 'antd';

const OpenCourse: React.FC<Props> = () => {
  return (
    <>
      <Card title="Filter">
        <p>Account Information</p>
      </Card>
      //Table show mon hoc, giao vien, so luong sinh vien, so luong dang ky, so luong da dang ky, so
      luong con lai, gio bi trungf
    </>
  );
};

type Props = {};

export default OpenCourse;
